## Decisions

- Palette is immutable.
- Color is immutable.
- Predefined colors are convenience constants.
- Custom colors are first-class citizens.
- API uses percentages instead of protocol values.
- Preset = A named, reusable State.